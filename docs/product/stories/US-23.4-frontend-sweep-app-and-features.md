# US-23.4: Frontend dead-code sweep — app & features

**Epic:** Epic 23 — Dead-Code Cleanup & Codebase Review
**PRD:** [`epic-23-dead-code-cleanup-and-review.md`](../prd/epic-23-dead-code-cleanup-and-review.md)
**Status:** Done
**Last updated:** 2026-06-17

## Story

As a **developer of this project**, I want the desktop frontend
(`src/app/`, `src/features/`) swept of confirmed-dead exports, components, types,
helpers, and CSS, and its smells catalogued, so that the UI codebase is only
live code rendered by a real surface.

## Context

The frontend is `src/app/` (state, storage, App shell, primitives) +
`src/features/portfolio/` (~32 non-test files); `src/features/market-data/` and
`src/features/settings/` appear empty/legacy. Known dead candidates: the
**admission-disposition persistence plumbing** in `portfolioWorkspaceStorage.ts`
(schema-sanitizing code with no producer/consumer — see the US-22.2 close-out)
and any unused exported types/helpers. Using `knip`/`ts-prune` + the removal
protocol, this story removes confirmed-dead TS/TSX and dead CSS tokens/classes,
and records FE smells (un-tokenised literals, prop-drilling, duplicated
formatters) into the register. **Persisted-state sanitizers are the trap**:
removing a sanitizer can corrupt or fail to migrate real IndexedDB/localStorage
state — only remove when the persisted field is provably gone from the type AND
no saved-state migration relies on it.

Implementer must read:
- `docs/tech-debt-register.md` + removal protocol (US-23.1).
- `src/app/portfolioWorkspaceStorage.ts` + `src/features/portfolio/workspaceTypes.ts`
  + `types.ts` — the disposition plumbing and persisted-state contracts.
- `.claude/skills/ui-polish/SKILL.md` + `src/test/designSystem.audit.test.ts` —
  the design-system token/primitive inventory (dead-CSS-token removal must keep
  the audit green).

## Acceptance criteria

- [x] AC1 — The **confirmed-dead** files/exports/types were removed: 6 unimported
  files (`featureFlags.ts`, `portfolioState.ts`, `CurrentFactorSnapshotCard.tsx`,
  `SectorDonutCard.tsx`, `historyTruth.ts`, `investorEconomics.ts`), 5 dead types
  (`ActivityPoint`, `CanonicalLedgerRecord`, `ReconciliationCheck`,
  `DiagnosticsPayload`, `ExposureEnginePayload`), and the dead helper
  `getPortfolioDatabaseName` (knip 7→1 files, 65→60 types, 22→21 exports). The
  remaining knip items are **live, over-exported** types (low-priority de-export,
  recorded) and `DashboardPerformanceChart.tsx` (test-coupled → **US-23.6**), both
  in the register — not dead-by-protocol deletions for this story.
- [x] AC2 — **Carved out to [US-23.9](US-23.9-remove-disposition-plumbing.md).**
  The disposition plumbing is a ~250-line persistence + 726-line-test + BE-schema
  change; per the 2026-06-17 decision it runs as its own focused slice gated by
  the workspace round-trip tests (stability), with the turnkey map in the
  register. Not removed here.
- [x] AC3 — `features/market-data` + `features/settings` contain **only a
  `README.md`** each — intentional placeholders for unbuilt features (documented
  repo layout in `CLAUDE.md`), not dead code. Left as-is + documented (register).
- [x] AC4 — **Documented, not removed.** A `var()`-reference scan flagged 7
  suspected-unused tokens, but several are documented design-scale tokens and
  others may be referenced indirectly; removing risks false positives + the
  design-system contract. Recorded in the register for a careful CSS pass; the
  design-system audit stays green (nothing removed).
- [x] AC5 — **No behaviour change**: full suite green after every removal;
  `npx tsc --noEmit` clean; dashboard goldens consumers unchanged.
- [x] AC6 — FE findings appended to `docs/tech-debt-register.md`: the dead-code
  triage, the over-export list, the suspected CSS tokens, and the "no FE
  anti-patterns of note" conclusion (post-Epic-12 token discipline).

## Test plan

Frontend (vitest):
- Full suite green after each removal; `portfolioWorkspaceStorage.test.ts` and
  `App.test.tsx` green (persisted-state round-trips intact).
- `designSystem.audit.test.ts` green after CSS-token removal.
- Tests covering only removed dead code are removed with it; no behavioural test
  weakened.

Regression / guardrail:
- A backend schema mirrored only by a removed TS type is reconciled (coordinate
  with US-23.2/23.5).
- `npx tsc --noEmit` clean; goldens untouched.

## Tickets

- [x] T-23.4.1 — **Dead exports/components/types sweep**: removed the 6 unimported
  files + 5 dead types + dead helper (knip-driven, protocol-confirmed); register
  populated. Suite + tsc green.
- [x] T-23.4.2 — **Legacy dirs + CSS + carve-outs**: empty feature dirs confirmed
  as documented placeholders (not dead); suspected-unused CSS tokens recorded for
  a careful pass; the disposition plumbing carved to **US-23.9** and
  `DashboardPerformanceChart` to **US-23.6** (both with turnkey register maps).
- [x] T-23.4.3 — **Docs close-out**: slice log (removal counts), register
  populated, carve-outs tracked; story → Done.

## Out of scope

- **Migrating the Concentration Pack / Dashboard tab to the design system** —
  explicitly deferred (ui-polish migration notes); only *dead* CSS is removed.
- **Fixing catalogued FE smells** — Epic 24.
- **Backend removals** — US-23.2/23.3.

## Notes / decisions

- **Persisted-state safety is the priority over deletion.** A sanitizer that
  guards malformed IndexedDB is *not* dead even if no feature produces the field
  — it protects load. The disposition plumbing is removable only because the
  field is being removed from the persisted type entirely; verify a pre-existing
  saved workspace (with or without the field) still loads.
- No new formula; no methodology change.
