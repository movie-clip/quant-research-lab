# US-11.3: Docs, contracts, roadmap close-out

**Epic:** Epic 11 — Factor Return Attribution
**PRD:** [`epic-11-factor-return-attribution.md`](../prd/epic-11-factor-return-attribution.md)
**Status:** Done
**Last updated:** 2026-05-27

## Story

As a **portfolio researcher** (and future implementer), I want the factor return attribution feature to be fully documented in the contracts, methodology, and roadmap, so that any field in the UI can be traced back to its backend source and formula without needing to read implementation code.

## Context

US-11.1 implemented the backend engine and schema; US-11.2 implemented the frontend card. This story closes the documentation loop: it creates the `docs/contracts/attribution-fields.md` field inventory (the backend ↔ TS ↔ UI contract for attribution), verifies the methodology section is complete in `financial-methodology.md`, updates the `epic-roadmap.md` slice log with all three story entries, and sets all US-11.x stories to Done.

The `financial-methodology.md` §Factor Return Attribution section was written during the research phase (quant-research skill) and should already be complete. This story verifies it and fills any gaps found during implementation.

Implementer must read:
- `docs/finance/financial-methodology.md` §Factor Return Attribution
- `services/quant-engine/app/schemas/attribution.py` — final as-built schema
- `apps/desktop/src/features/portfolio/types.ts` — final attribution TS types
- All three US-11.x story files — to confirm all ACs are ticked before setting Done

## Acceptance criteria

- [x] AC1 — `docs/contracts/attribution-fields.md` exists and contains a field inventory table with every field in `FactorAttributionResponse` and its nested types, with columns: Field | Backend type (Python) | TS type | UI label | Trust class | Nullable.
- [x] AC2 — `docs/finance/financial-methodology.md` §Factor Return Attribution contains all formulas (daily contribution, daily residual, period sums, cumulative series, reconciliation identity), all edge cases, and all three academic citations (Brinson 1986, Fama & French 1993, Bacon 2008).
- [x] AC3 — `docs/product/epic-roadmap.md` slice log contains one entry per US-11.x story, each with the date shipped, the story ID, and a one-line description of what shipped.
- [x] AC4 — `docs/product/epic-roadmap.md` story snapshot table shows all three US-11.x stories as "Done" and Epic 11 status updated to "Completed".
- [x] AC5 — `docs/product/stories/US-11.1-attribution-engine-endpoint.md` status is `Done` and all ticket checkboxes are ticked.
- [x] AC6 — `docs/product/stories/US-11.2-attribution-card-chart-table.md` status is `Done` and all ticket checkboxes are ticked.
- [x] AC7 — `docs/product/stories/US-11.3-attribution-docs-closeout.md` status is `Done` and all ticket checkboxes are ticked.
- [x] AC8 — `python scripts/run_all_tests.py` remains green after all doc edits (no accidental file corruption).

## Test plan

No new code tests in this story. Verification is doc-review:

Regression / guardrail:
- `python scripts/run_all_tests.py` green — doc-only changes must not break anything
- Manual check: every field in `attribution-fields.md` matches the as-built schema in `attribution.py` and `types.ts` — no field invented, no field missing
- Manual check: no field in `attribution-fields.md` uses the label "alpha" — must be "unexplained" or "idiosyncratic"

## Tickets

- [x] T-11.3.1 — **Contract doc**: create `docs/contracts/attribution-fields.md` with the full field inventory table derived from the as-built `attribution.py` schema and `types.ts` types.
- [x] T-11.3.2 — **Methodology verification**: read `docs/finance/financial-methodology.md` §Factor Return Attribution; confirm all formulas and citations are present as written in the research phase; patch any gaps found during US-11.1/11.2 implementation.
- [x] T-11.3.3 — **Roadmap + story closeout**: add slice-log entries to `epic-roadmap.md` for US-11.1, US-11.2, US-11.3; update Epic 11 status to Completed; set all three story files to Done with tickets ticked.

## Out of scope

- No code changes — this is a documentation-only story.
- No new methodology sections beyond §Factor Return Attribution.
- No changes to other contract docs (`drift-fields.md`, `exposure-fields.md`, etc.).

## Notes / decisions

- **`attribution-fields.md` format**: follow the pattern of `docs/contracts/drift-fields.md` or the nearest existing contract doc. Each row maps one backend field to its TS counterpart and its UI rendering location.
- **Trust class column**: every attribution field is synthetic history. The contract doc should state this once in a preamble and again in the Trust class column for each field.
- **Completion gate**: this story may not be set to Done until `python scripts/run_all_tests.py` is green and both US-11.1 and US-11.2 have all acceptance criteria ticked. Do not close the epic if any AC from a prior story is unticked.
